version: '3.3'

services:
    backend:
        build:
            context: ./backend
        volumes: 
            - ./backend:/app
        command: /start.sh
        restart: on-failure
        ports: 
            - "8001:8000"